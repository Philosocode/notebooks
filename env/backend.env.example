PORT=5000
NODE_ENV=development
# for docker-compose: `DB_HOST=db` to match the DB service name
DB_HOST=db

# should match the one in frontend.env
OAUTH_CLIENT_ID=your_client_id

JWT_SECRET=your_jwt_secret

# JWT expiry time in days
# e.g. 90 means 90 days
JWT_EXPIRES_IN=90
